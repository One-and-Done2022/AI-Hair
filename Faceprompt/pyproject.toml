[project]
name = "faceprompt"
version = "0.1.0"
description = "Structured portrait scene and hairstyle prompt catalog for Chinese image models."
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
