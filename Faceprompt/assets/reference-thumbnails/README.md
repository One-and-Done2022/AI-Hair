# Internal Reference Thumbnails

这个目录预留给内部低分辨率参考缩略图，不直接参与程序运行。

使用规则：

1. 仅保存用于策划和人工校对的低分辨率缩略图，不存原始高清图。
2. 文件名使用 `source-id__short-description.ext` 形式，例如 `gq-high-fade__front-view.jpg`。
3. 每新增一张图，都需要同时在来源文档里记录原始链接、抓取日期和用途。
4. 这类缩略图默认只适用于私有仓库；若仓库转为公开，请移除图片，仅保留外链和文字特征提炼。
